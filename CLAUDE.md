## 環境変更の鉄則

**`~/.claude/` を直接編集してはいけない。**

- すべての設定・スクリプト変更は chezmoi ソース `~/.local/share/chezmoi/dot_claude/` を編集する
- 変更後は必ず `chezmoi apply` で反映する
- `~/.claude/` 配下の直接編集は chezmoi の管理と乖離し、`chezmoi apply` で上書きされる
- ランタイムデータ（ログ・セッション分析結果）は `~/.claude/tasks/` に書いてよい（chezmoi 管理外）

**`~/.local/share/chezmoi/dot_claude/` の変更は事前確認なしで即実施してよい。**

- ユーザーは chezmoi / dot_claude 配下の編集を事前に許可している
- `allow リスト追加時のルール`（副作用先出し）は dot_claude 変更には適用しない
- chezmoi apply と push まで一連でやりきる

---

## 改善の優先順位

問題・非効率を発見したときの改善アプローチは以下の順序で検討する:

1. **Hook化** — `PreToolUse` / `Stop` / `UserPromptSubmit` フックで自動化できれば最も堅牢
2. **Allow リスト更新** — 繰り返し承認が必要なコマンドを `settings.json` に追加して承認をゼロにする
3. **Skill化** — 繰り返し使うエージェント起動パターンをスキルにまとめる
4. **Rule（Prompt最適化）** — CLAUDE.md に明示的なルールを追記して Claude の行動を変える

上位で解決できるものを下位で解決しようとしない。

---

## 改善報告の決定論的フォーマット

ユーザー指摘・自発的に発見した改善を反映する際は、**以下のテンプレを毎回そのまま** 使う。同じテンプレを次の 3 箇所で統一する:

1. ユーザーへのチャット応答（「改善しました」と報告するとき）
2. Stop hook の改善マーカー (`/tmp/claude-improvements-<sid>.md`)
3. `tasks/<task>/timeline.md` の改善ログ

### テンプレ（改善 1 件あたり）

```
### 改善 #N: <1 行サマリ>

- **指摘 / 動機**: <ユーザー指摘の引用 or 発見した課題の 1 文>
- **改善の種類**: Hook / Allow / Skill / Rule / 別途調査
- **改善先（場所）**: <絶対パス or リポジトリ相対パス。複数なら箇条書き>
- **その場所を選んだ理由**: <改善の優先順位 (Hook > Allow > Skill > Rule) に照らした 1〜2 文>
- **実装内容**: <1〜3 行の要点>
- **永続化**: <commit hash / push 済 / chezmoi apply 済 / 未反映>
```

必須フィールドは **6 つ全部**。`Stop` hook の検証ロジックがマーカーを正規表現で grep し、`### 改善 #N:` ヘッダー数と各フィールド `- **<name>**:` の出現数を比較する。欠落があれば exit 2 で書き直しを要求する。

### 例

```
### 改善 #1: gh pr create を makinzm 以外向けでブロック

- **指摘 / 動機**: 「PR 勝手に作るな。他のリポジトリに関連するのがダメってことね」
- **改善の種類**: Hook
- **改善先（場所）**:
  - `~/.local/share/chezmoi/dot_claude/scripts/executable_no-pr-create.sh`
  - `~/.local/share/chezmoi/dot_claude/settings.json` (PreToolUse Bash matcher)
- **その場所を選んだ理由**: 優先順位最上位の Hook で `gh pr create` を機械的にブロックすれば再発防止が確実。Rule では Claude が忘れる可能性があり、外部リポジトリへの誤投下のような high-blast-radius な事故は防げない。
- **実装内容**: `--repo OWNER/REPO` から owner を抽出（無ければ origin URL から）し、`makinzm` 以外なら exit 2 でブロック。
- **永続化**: commit `d5072fe` (dot_claude) / `e2313e0` (parent chezmoi)、両方 push & chezmoi apply 済。
```

「指摘ゼロ」のセッションは `### 改善 #N:` ヘッダー自体を書かず、本文に `指摘なし` と明記する（検証はスキップされる）。

---

## 承認・コンテキスト削減の原則

タスク実行中は常に **「この承認は次回なくせるか？」** を考える。

- 繰り返し承認が必要なコマンドは即座に `settings.json` の `permissions.allow` に追加する
- allow リストの更新も chezmoi ソース経由で行い、`chezmoi apply` で反映する
- Stop フックが発動したとき: 承認ログを確認し、パターンがあれば **allow リストを更新してからマーカーファイルを書く**

### Stop フック対応手順

Stop フックが `【セッション改善チェック】` を出力したとき:

1. 表示された承認ログを確認する
2. 安全に自動承認できるパターンを特定する
3. `~/.local/share/chezmoi/dot_claude/settings.json` の `permissions.allow` に追記する
4. `chezmoi apply` を実行する
5. **ユーザー指摘の棚卸し** — そのセッション中にユーザーから受けた指摘・修正・「やめて」「違う」「やり直し」「勝手に」等のフィードバックを箇条書きで列挙し、それぞれの改善先（Hook / Allow / Skill / Rule / 別途調査）を決める。指摘ゼロの場合のみ「指摘なし」と明記する。
6. 改善内容（または「改善はない」）をマーカーファイルに書く
7. **マーカーファイルを書いても作業は終わらない。BACKLOG に未完了タスク（`[ ]` / `[~]`）があれば即座に次のタスクへ進む。Stop フック対応は中断点ではなく通過点。ユーザーを介入させない。**

### allow リスト追加時のルール

allow リストへ項目を追加する前に、ユーザーへ以下を **必ず先出し** する：

- **副作用の有無** — 読み取り / 書き込み / ネットワーク / 他システム影響の有無
- **追加理由** — どのワークフローで承認が頻発したか
- **拒否時の代替** — 追加しなかった場合の運用

提示なしで edit に進むのは禁止。「とりあえず追加してみる」を独断で行わない。

---

## トークン・コスト効率化の原則

承認削減（上記）とは別に、**トークン消費そのもの**を減らす工夫を作業の随所で行う。以下のループエンジニアリング・自律タスク発見ループの各ステージから、該当箇所でこの節を参照する。

### コンテキストの汚染を避ける
- L/XLタスクの実装はメインコンテキストから `developer` / `tdd-pr-workflow` agent に分離する（[エージェントとスキルの活用](#エージェントとスキルの活用)）。メインの会話に大量のログ・diffを残さない
- サブエージェントには「全部読んで」ではなく対象ファイル・行範囲・diffだけを渡す。サブエージェントの出力もログ全文ではなく要約で返してもらう
- 一度読んで状態を把握したファイルを、Edit/Write成功後に確認のためだけに再Readしない（ツールが失敗すればエラーで分かるため）

### 読み取りを絞る
- 大きいファイルは全文Readせず、`grep` / `offset` + `limit` で必要な範囲だけ読む
- ディレクトリ探索は `find` / `grep` を直接使う。3クエリを超えて広がりそうな探索は `Explore` agent に委譲してメインコンテキストを汚さない
- 同じ内容を別の角度から何度も読み直さない。すでに会話内で得た情報は再取得せず再利用する

### ツール呼び出しの往復を減らす
- 依存関係のないツール呼び出しは1メッセージ内で並列実行し、往復回数を減らす
- `Agent` / `Workflow` の `model` / `effort` はタスクの難易度に対して必要十分なものを選ぶ。理由なく `opus` や `high`/`max` effort を既定にしない

### 自律タスク発見ループでの注意
- `ScheduleWakeup` の間隔はキャッシュ窓（5分）を意識する。アイドル時は20〜30分、外部状態の変化を追うときだけ短い間隔にする。300秒前後の中途半端な間隔は避ける
- ウェイクアップごとに会話全体を再構築させない。タスクIDのディレクトリに状態を書き残し、次回はそこから差分だけ読む

### 報告・記録の簡潔化
- 改善報告・合意事項はチャットで毎回全文を再掲せず、`tasks/` 配下のファイルに書いて「このファイルを見てください」で済ませる
- ユーザーへの報告は結論と次の一手を中心に、過程の詳細はファイル参照に譲る

---

## ループエンジニアリング（全体フロー）

このプロジェクトの開発プロセスは「ループエンジニアリング」——人間の意図を起点に、明確化・承認・実装・独立レビュー・出荷・フィードバック反映を1サイクルとして繰り返す——という考え方に基づく。各ステージは既にこのファイルの個別セクションで実装されているので、ここでは全体像と「どのステージで何を見ればいいか」だけをまとめる。新しいルールを増やすのではなく、既存セクションの位置づけを明確にするための索引として使う。

### サイクル図

```
人間の要望
  → ① インテイク（要望を受け取る）
  → ② 明確化（不明瞭なら詰める）
  → ③ 人間承認（詰めた内容を承認してもらう）
  → ④ 実装（テストファーストで作る）
  → ⑤ 独立レビュー（別コンテキストのAgentでレビューループ）
  → ⑥ 出荷（PR作成など）
  → ⑦ フィードバック反映（人間の指摘を次サイクルの改善に変換）
  → ① に戻る
```

### 各ステージの参照先

| ステージ | 何をするか | 参照セクション / ファイル |
|---|---|---|
| ① インテイク | 要望をそのまま受け取る。勝手に解釈・拡大しない | `po` agent (`agents/po.md`) |
| ② 明確化 | 曖昧な点を Q&A 形式で詰める。解消するまで実装に進まない | 本セクション直後の「2. 計画時の疑問点Q&A」 |
| ③ 人間承認 | 詰めた計画を提示し、明示的な承認を得る | 「1. 計画モードをデフォルトに」、`agents/tdd-pr-workflow.md` フェーズ1-B |
| ④ 実装 | テストファースト・TDDで実装する | `プロセス原則`、`agents/developer.md` |
| ⑤ 独立レビュー | 実装者と別コンテキストのAgentでレビューループを回す | `Devil's Advocate レビューループ`、`agents/da-reviewer.md` |
| ⑥ 出荷 | PR作成・チェックリスト消化 | `agents/tdd-pr-workflow.md` フェーズ3 |
| ⑦ フィードバック反映 | 人間からの指摘を改善アクションに変換し、次サイクルのインプットにする | `改善報告の決定論的フォーマット`、「3. 自己改善ループ」 |

①〜⑦のどのステージでも、サブエージェント分離・読み取り範囲の絞り込み・並列実行などで [トークン・コスト効率化の原則](#トークンコスト効率化の原則) を適用する。

このループは1往復で終わらなくてよい。⑤で指摘が出れば④に戻り、出荷後に人間から新たな指摘が出れば②から再度回す。自律タスク発見ループ（後述）から着手するタスクも、必ず②以降のこのサイクルに合流させる。

---

## ワークフロー設計

### 1. 計画モードをデフォルトに
- 3ステップ以上のタスクやアーキテクチャ判断は、必ずPlanモードから始める
- うまくいかなくなったら、無理に進めず即座に再計画する
- ビルドだけでなくバリデーションにもPlanモードを活用する
- 実装前に詳細な仕様を書いて曖昧さを減らす

### 2. 計画時の疑問点Q&A

計画フェーズで不明点や判断に迷う点があれば、`tasks/YYYYMMDD-<タイトル>/QA.md` にやりとりを記録する。

```markdown
## 🤔 疑問点・確認事項

### Q1: <疑問の内容>
- **背景**: なぜこの疑問が生じたか
- **選択肢**:
  - A: <案A> — メリット / デメリット
  - B: <案B> — メリット / デメリット
- **回答**: （ユーザーの回答をここに記録）
- **決定**: （最終的な判断）

### Q2: ...
```

- 疑問が解消するまで実装に進まない
- 回答と決定は必ず記録し、後からレビュワーが参照できるようにする
- 判断に詰まったら人間に聞く — 勝手に決めない

### 3. 自己改善ループ
- 修正を受けたら、パターンを自分の memory（`/Users/hope/.claude/projects/*/memory/`）に記録する
- 同じ間違いを繰り返さないためのルールを自分で書く
- エラー率が下がるまでルールを磨き続ける

### 4. エレガンスの追求（バランスを取りつつ）
- 大きな変更の前に「もっとエレガントなアプローチはないか？」と立ち止まる
- ハックっぽい修正なら「今の知識を総動員して、エレガントな解決策を考える」
- 単純で明らかな修正にはこのプロセスをスキップ — 過剰設計しない
- 提示前に自分の仕事を自己批評する

### 5. 自律的バグ修正
- バグ報告を受けたら、手順を教えてもらうのを待たず直接修正する（ただし、修正方針に複数の選択肢があり判断に迷う場合はQ&Aプロセスに従う）
- ログ、エラー、失敗テストから自分で診断する
- ユーザーのコンテキストスイッチをゼロにする
- CIテストの失敗は聞かれなくても自発的に修正する

### 6. テストファースト開発

コードを書くときは `writing-code` スキルのベストプラクティスを確認すること。常にテストを先に書いてから実装コードを書く。このテストファーストアプローチにより、明確な仕様に向かって作業でき、問題を早期に発見できる。

### 7. セッション開始時のバックログ確認

`UserPromptSubmit` フックがセッション初回に `docs/tasks/BACKLOG.md`（なければ `BACKLOG.md`）を自動注入する。
注入されたバックログを読んで、`[~]` 進行中タスクと `[ ]` 未着手タスクの優先順位を把握してから作業に入ること。
バックログが存在しないプロジェクトで複数セッションにまたがる作業が生じた場合は、作業開始前に `docs/tasks/BACKLOG.md` の作成を提案する。

---

## Devil's Advocate レビューループ

タスク実行時は、**実装者**と**レビュワー（Devil's Advocate）**のAgentを分離する。

### ループの流れ

```
実装者Agent → 実装完了 → DAレビュワーAgent（レビュー）
     ↑                              ↓
     └──── 修正 ←── 指摘事項あり ←──┘
                     指摘なし → 完了
```

### 実装者Agent の役割
- コードの実装・テスト作成・リファクタリングを担当
- DAからの指摘を受けて修正する
- 修正後、再度DAにレビューを依頼する

### DAレビュワーAgent の役割
- 実装者の変更を**批判的に**レビューする
- 以下の観点でチェック:
  - バグや論理ミスがないか
  - テストが十分か（エッジケース含む）
  - セキュリティ上の問題がないか
  - コードの可読性・保守性
  - CLAUDE.md のプロセス原則に従っているか
- 指摘事項を具体的に列挙する
- 問題がなければ「LGTM」で承認する

### 起動方法
- 実装者（メインAgent or tdd-pr-workflow）が実装完了後、`da-reviewer` Agentをサブプロセスとして起動する
- Agent ツールで `subagent_type: "da-reviewer"` を指定して呼び出す
- tdd-pr-workflow を使う場合は、フェーズ2（TDDサイクル）完了後・フェーズ3（PR作成）前に自動的にDAレビューが挟まる

### 運用ルール
- DAは最低1回はレビューを実施する
- DAの指摘に対して実装者が修正したら、再度DAがレビューする
- 3往復しても収束しない場合は、人間に判断を仰ぐ
- DAレビューの結果は `tasks/YYYYMMDD-<タイトル>/da-review.md` に記録する

### DAレビュー記録フォーマット

レビュー結果は `tasks/YYYYMMDD-<タイトル>/da-review.md` に記録する。
フォーマットの詳細は `agents/da-reviewer.md` を参照。

---

## 自律タスク発見ループ（オートパイロット）

ユーザーが「自動でタスクを探して」「バックグラウンドで回しておいて」のように明示的に自律モードを指示したときだけ使う。指示がない限り自分からこのモードに入らない。

### 起動方法

組み込みの `/loop`（インターバル指定なしの自己ペース版）と `ScheduleWakeup` ツールを使う。このループ専用の hook や常駐プロセスを新たに自作しない（[改善の優先順位](#改善の優先順位) のとおり既存の仕組みを優先する）。ウェイクアップ間隔は [トークン・コスト効率化の原則](#トークンコスト効率化の原則) の「自律タスク発見ループでの注意」に従う。

### 各サイクルでやること

1. **タスク発見（GitHubを含む）**: 以下から候補を洗い出す
   - `gh issue list` / `gh pr list --search "review-requested:@me"` 等、自分（makinzm）のリポジトリの未対応issue・レビュー待ちPR
   - 失敗している CI（`gh run list --status failure` 等、自分のリポジトリのみ）
   - `tasks/*/TODO.md` の未完了チェックボックス
   - リポジトリ内の `TODO` / `FIXME` コメント
   - 直前のセッションで「指摘あり・未対応」のまま残っている改善項目
2. **優先度判定 → 提案**: 期限の明示があるもの > ブロッカー解消（レビュー待ち・CI赤） > ユーザー明示の優先度 > 技術的負債、の順で並べる。判断が割れる場合はユーザーに聞く。優先度を独断で確定させない。並べた結果から「次はこれをやるとよさそうです」という提案を1〜3件、根拠（issue番号・CI失敗内容など）付きで提示する。
3. **着手前チェックポイント（必須・スキップ禁止）**: 提案の中からユーザーが選ぶ、またはユーザーが追認するまで実装に入らない。「これから〇〇に着手します。理由: 〜」を一言で提示し、ユーザーが止める機会を作る。フルの `QA.md` までは不要だが、無言で実装を始めてはいけない。
4. **タスクIDの確定**: 着手するタスクに `tasks/YYYYMMDD-<タイトル>/` のディレクトリ名をそのままタスクID（一意なslug）として割り当てる。新しいID発行の仕組みは作らず、既存のディレクトリ命名規則をIDとして使う。
5. **本サイクルへの合流**: 確定したタスクは [ループエンジニアリング（全体フロー）](#ループエンジニアリング全体フロー) の②（明確化）以降に通常通り合流させる。自律モードだからといって明確化・承認・独立レビューを省略しない。
6. **次回ウェイクアップの予約**: 完了後 `ScheduleWakeup` で次回チェックのタイミングを設定する。何も状態が変わらない待機は20〜30分、CI完了待ちのような短時間で状態が変わるものは数分単位にする。

### 守ること

- 自律モードでも「mainブランチでは作業しない」「他人リポジトリへの `gh pr create` 禁止」等の既存の安全ルールは免除されない
- ユーザーが停止を指示したら（`/goal clear` 含む）即座に止める
- 1サイクルで新しい候補が見つからなければ、その旨をユーザーに報告してからウェイクアップ間隔を伸ばす。無言で回し続けない

### タスクIDとエージェント間の引き継ぎ

タスクごとに一意なIDを発行し、作業履歴をそのID配下に集約することで、別コンテキストのAgent（DAレビュワー・developer・自律ループ自身の再起動など）が同じ作業を追えるようにする。

- **タスクID = `tasks/YYYYMMDD-<タイトル>/`**: 新規のID発行基盤は作らない。既存のタスクディレクトリ命名規則がそのままユニークIDとして機能する（日付+タイトルで衝突しない）。
- **履歴の集約**: `TODO.md` / `QA.md` / `timeline.md` / `da-review.md` をすべて同じタスクID配下に置く。別コンテキストのAgentはこのディレクトリを読めば作業履歴の全体を復元できる。
- **Agent間の参照**: `Agent` / `Workflow` ツールでサブエージェントを起動するときは、`name` パラメータにタスクID（例: `20260625-loop-engineering`）を使う。同じタスクIDで `SendMessage({to: <タスクID>})` すれば、別コンテキストのAgentに同じ作業の続きを依頼できる。
- **自律ループからの合流時も同様**: ステップ4で確定したタスクIDをそのまま使い、提案文・チェックポイントの一言報告にもタスクIDを含める。

---

## プロセス原則

1. mainブランチでは作業しない。必ず新しいブランチを切る。ビルド破壊を防ぎ、履歴をきれいに保つため。タスクや機能を反映した分かりやすいブランチ名をつける。
2. 最初に `tasks/YYYYMMDD-<タイトル>/TODO.md` を作成し、タスクの詳細を書き出す。要件と手順を明確にし、レビュワーがコンテキストを理解できるようにする。
3. レビュワー用のタイムラインログを `tasks/YYYYMMDD-<タイトル>/timeline.md` に作成する。思考プロセスや遭遇した課題の記録になる。
4. 実装コードの前に必ずテストコードを書く。テストファーストにより明確な仕様に向かって作業でき、問題を早期に発見できる。テスト実装時のエラーログをtimelineに記録し、テストファーストを実践していることを示す。
5. コードを書く。
6. 動く実装ができたらリファクタリングと最適化を行う。最初から完璧にしようとせず、まず動くものを作ってから改善する。
7. テストやそれに類するプロセスがCIで自動実行されるようにする。
8. `git push` 後は `gh run watch --exit-status` で CI の全ジョブが green になるまで待ち、確認してから完了報告する。CI が失敗したら即座に修正して再プッシュし、再度 green を確認する。

### 実装完了 → DA レビュー前のチェックリスト

実装ファイルに変更を加えた場合、DA レビューに渡す前に以下を **必ず** 実行し、結果を `tasks/YYYYMMDD-<タイトル>/timeline.md` に貼る:

1. **テスト**: 言語別 — `cargo test` / `pytest` / `npm test` / `go test` / 等
2. **lint**: 言語別 — `cargo clippy --tests -- -D warnings` / `ruff check` / `eslint` / `mypy` / `pyright` / 等
3. **フォーマット**: 言語別 — `cargo fmt --check` / `ruff format --check` / `prettier --check` / 等

3 つすべて clean になるまで DA レビューに進まない。clean でないままレビュー依頼するのは禁止。

`Stop` フックは「実装変更あり（10 行以上）かつ lint 系コマンドの実行ログなし」を検出すると `⚠ lint / formatter 未実行` と警告する。警告が出たまま完了させない。

---

## エージェントとスキルの活用

### エージェント一覧（`agents/` が単一の情報源）

| エージェント | 役割 | 使うタイミング |
|---|---|---|
| `architect` | 設計・ADR・技術選定 | 大規模変更の前、技術選定、アーキテクチャレビュー |
| `developer` | TDD実装・バグ修正 | L/XLタスクの実装をメインコンテキストから分離したいとき |
| `po` | 要件整理・ユーザーストーリー | 要件が曖昧で仕様を固めたいとき |
| `da-reviewer` | 批判的レビュー | 実装完了後・PR前のレビュー |

### スキル（`/スキル名` で起動）

スキルはエージェントを起動するための薄いラッパー。実体定義は `agents/` のファイルにある。

- `/architect` → architect agent 起動
- `/developer` → developer agent 起動
- `/po` → po agent 起動
- `/da-review` → da-reviewer agent 起動

### タスクサイズとエージェント使い分け

- **S/M**: メインClaudeが直接対応
- **L**: メインClaudeが対応。必要に応じてサブエージェントを活用
- **XL**: `developer` または `tdd-pr-workflow` agent に委譲（コンテキスト汚染防止）

タスクサイズは `UserPromptSubmit` フックが自動推定してコンテキストに注入する。

### 自動フック一覧

| フック | タイミング | 効果 |
|---|---|---|
| `UserPromptSubmit` | ユーザー入力時 | 難易度推定(S/M/L/XL)、L/XL時は状態ファイル作成 |
| `PreToolUse` (全) | ツール実行前 | 承認音（allow 対象外ツールを検出してログ記録）+ ループ検出 |
| `PreToolUse` (Write/Edit) | ファイル書き込み前 | TDDガード（テスト未実行なら実装ファイル書き込みブロック） |
| `Stop` | Claude応答完了後 | 承認ログ付き改善チェック強制（exit 2 でブロック）→ 完了通知 |

---

## 他プロジェクト / 公開ライブラリへの貢献

外部リポジトリ（pola-rs/polars 等の他人のリポジトリ）に PR を出す前提で実装する場合、以下を必ず守る。

### 実装前の調査義務

- **既存 API の確認**: 変更領域に関連する既存の public API・命名規則を必ず調査する。公式ドキュメント、参照実装（pandas 等の競合）と並べて見て、命名衝突・機能重複・慣習からの逸脱がないかを確認してから実装に入る。
- **issue / 親 PR の精読**: 親 issue / 関連 PR を必ず読み、目的・スコープ・受け入れ条件を明確にする。「kernel only か API まで か」「動的窓 vs 固定窓」のような暗黙の前提を実装前に確定させる。曖昧なら実装に入らず issue に質問する。

### 設計合意の義務

- **I/F・命名・スコープはユーザー合意必須**: 関数シグネチャ、引数順序、引数名、命名規則、PR スコープを独断で決めない。案を提示してユーザー判断を仰ぐ。
- **複数の選択肢を必ず列挙**: 「内部最適化のみ / 新 API 追加 / 両方」のような軸で複数案を並べる。1 案だけ提示して進めるのは避ける（ユーザーが他選択肢を知らないまま同意してしまう）。
- **「すでに動いているか / 何が変わるか」を最初に確認**: 「内部最適化（外部に見える挙動は不変）」と「新 API 追加（surface が増える）」を取り違えると致命的。設計時に必ず軸として意識する。

### コーディング規約の遵守

- **既存スタイル踏襲**: コードコメントを書く前に同ディレクトリ・同プロジェクトの既存スタイルを `grep` 等で確認する。長文 NOTE を独断で導入しない。1〜2 行で書けないなら、その内容はコード外（PR description / ADR / issue body）に書くべき。
- **長い議論はコード外**: 設計判断・代替案・履歴は PR description / ADR / issue body に書く。コードに長文ブロックコメントを置かない。

### 文書の自己完結性

- **チャット内ラベルを文書で使わない**: 議論の中で生まれた略号（A/B/C 案、「前回の」「先ほどの」）を外部文書（issue, PR, ADR, コメント）でそのまま使わない。各案を完全な文として展開する。
- **前提知識なしで読めること**: 文書を未来の自分・第三者が単独で読んで意味が通る形にする。

### 公開アクションの確認

- 他人リポジトリへの `gh pr create` は hook (`no-pr-create.sh`) でブロック済み。
- それ以外（`gh issue create`, `gh pr comment`, `gh pr close` 等）も、upstream / 他人のリポジトリに対するものは事前に内容を提示してユーザー判断を仰ぐ。自分のリポジトリ（makinzm 所有）への操作は通常通り進めて良い。

---

## コア原則

- **シンプルさ最優先**: すべての変更をできるだけシンプルに。影響するコードを最小限に。
- **近道なし**: 根本原因を見つける。一時的な修正を避ける。シニアエンジニアの基準で自分を律する。
- **影響範囲の最小化**: 必要なところだけ変更する。新しいバグを持ち込まない。
- **外部依存のバージョン確認**: GitHub Actionsやライブラリなどの外部依存を追加・更新する際は、記憶に頼らず必ずリリースタグや公式ドキュメントで最新バージョンを確認する。
